import 'package:flutter/material.dart';

class Principle extends StatelessWidget {
  const Principle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Principle");
  }
}
